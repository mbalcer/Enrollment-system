import {FieldOfStudyPipe} from './field-of-study.pipe';

describe('FieldOfStudyPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldOfStudyPipe();
    expect(pipe).toBeTruthy();
  });
});
