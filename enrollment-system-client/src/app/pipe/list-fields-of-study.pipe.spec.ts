import {ListFieldsOfStudyPipe} from './list-fields-of-study.pipe';

describe('ListFieldsOfStudyPipe', () => {
  it('create an instance', () => {
    const pipe = new ListFieldsOfStudyPipe();
    expect(pipe).toBeTruthy();
  });
});
